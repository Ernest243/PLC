public class Test
{
    // Variables
    public int i, n, j, x;

    // Labeled loop
    reject: for(i = 1; i <= n; i++)
    {
        for(j = 1; j = n; j++)
        {
            if(x[i][j] != 0)
                break;
            System.out.println("First all-zero row is: " + i);
            break reject;
        }
    }
}